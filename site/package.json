{
  "name": "open-rail-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "mkdir -p public/media && cp -rf ../data/media/robots public/media/",
    "dev": "astro dev",
    "prebuild": "mkdir -p public/media && cp -rf ../data/media/robots public/media/",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.34.0",
    "@astrojs/tailwind": "^6.0.0",
    "astro": "^5.0.0",
    "tailwindcss": "^3.4.0"
  }
}
